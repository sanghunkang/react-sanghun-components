import React from 'react';

// export default function ResponseChainColumn() {
//   return(
//     <g>
//       <rect
//         className="response-chain__column"
//         x={svgPadding + findColumnWidth(svgWidth, svgSpacing, Object.entries(consolidatedTransitions).length) * i + svgSpacing * i}
//         y={svgHeight * 0.05}
//         width={findColumnWidth(svgWidth, svgSpacing, Object.entries(consolidatedTransitions).length)}
//         height={findColumnHeight(svgHeight, svgPaddingVertical)}
//       />
//     </g>
//   );
// }